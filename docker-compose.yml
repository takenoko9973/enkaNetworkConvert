services:
    enka_network-translate:
        build:
            context: .
            dockerfile: .devcontainer/Dockerfile
        init: true
        container_name: enka_network-translate
        working_dir: /workspace
        volumes:
            - ./:/workspace
            - node-modules:/workspace/node_modules
        tty: true

volumes:
    node-modules:
